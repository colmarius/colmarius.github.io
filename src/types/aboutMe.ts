export type WorkExperienceProps = {
  period: string;
  title: string;
  company: string;
  link: string;
};

export type EducationProps = {
  period: string;
  title: string;
  university: string;
};
