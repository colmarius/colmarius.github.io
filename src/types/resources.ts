export type Newsletter = {
  id: number;
  name: string;
  url: string;
  description: string;
  image: string;
};

export type Book = {
  id: number;
  title: string;
  author: string;
  url: string;
  description: string;
  image: string;
  category: string;
};
